# MadArtist TxtParse.Parser 模块文档
这是一个用于解析文本的模块，以下是自定义一个模块所需的内容。
## \_\_init\_\_.py

- **Func parse_txts**: 解析文本列表，返回解析后的数据。
```python
def parse_txts(txts: list[str]) -> list[str]:
```
### 文件结构如下所示
```python
def parse_txts(txts: list[str]) -> list[str]:
    ...
```