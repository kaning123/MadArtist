# Doc for MadArtist TxtParse Parser
This is a guide of making a custom Txt Parser. 
## \_\_init\_\_.py

- **Func parse_txts**: The function of parsing texts to strings which can be used in the BaseVoiceGenerator.
```python
def parse_txts(txts: list[str]) -> list[str]:
```
### The File is written like below:
```python
def parse_txts(txts: list[str]) -> list[str]:
    ...
```